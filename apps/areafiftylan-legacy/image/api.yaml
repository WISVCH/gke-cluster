apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: areafiftylan-legacy-api
  namespace: flux-system
spec:
  image: ghcr.io/areafiftylan/lancie-api
  interval: 5m0s
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: areafiftylan-legacy-api
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: areafiftylan-legacy-api
  filterTags:
    pattern: "^(?P<ts>.*)-[a-fA-F0-9]+"
    extract: "$ts"
  policy:
    numerical:
      order: asc
