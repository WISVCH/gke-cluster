apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: wiki
  namespace: default
spec:
  interval: 15m
  chart:
    spec:
      chart: ./wiki
      version: "0.2.x"
      interval: 15m
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: wisvch
        namespace: flux-system
  values:
    replicaCount: 1
    tlsSecret: wisvch-tls
    domain: wiki.wisv.ch
    image:
      repository: ghcr.io/wisvch/wiki
      tag: 20230215-8d46de6 # {"$imagepolicy": "flux-system:wiki:tag"}
  valuesFrom:
    - kind: Secret
      name: wiki
      valuesKey: connect-client-secret
      targetPath: oauth.client.secret
    - kind: Secret
      name: wiki
      valuesKey: connect-client-id
      targetPath: oauth.client.id
