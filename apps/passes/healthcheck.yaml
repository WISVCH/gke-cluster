apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: passes
  namespace: default
spec:
  default:
    config:
      type: HTTP
      httpHealthCheck:
        port: 3000
        requestPath: /
    logConfig:
      enabled: true
  targetRef:
    group: ""
    kind: Service
    name: passes
