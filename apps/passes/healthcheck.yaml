apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: passes
  namespace: default
spec:
  default:
    config:
      type: HTTP
      httpHealthCheck:
        port: 80
        requestPath: /passes
    logConfig:
      enabled: true
  targetRef:
    group: ""
    kind: Service
    name: passes
