apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: connect
  namespace: default
spec:
  parentRefs:
  - kind: Gateway
    name: gateway
  hostnames:
  - connect.wisv.ch
  rules:
  - backendRefs:
    - name: connect
      port: 80
