apiVersion: v1
kind: Service
metadata:
  labels:
    app: match-rally
  name: match-rally
  namespace: default
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: match-rally
  sessionAffinity: None
  type: ClusterIP