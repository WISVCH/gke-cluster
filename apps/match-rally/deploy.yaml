apiVersion: apps/v1
kind: Deployment
metadata:
  name: match-rally
  namespace: default
  labels:
    app: match-rally
spec:
  replicas: 1
  selector:
    matchLabels:
      app: match-rally
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      name: match-rally
      labels:
        app: match-rally
    spec:
      containers:
        - name: match-rally
          image: ghcr.io/wisvch/annu-game-69:20260502-a57018e # {"$imagepolicy": "flux-system:match-rally"}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            successThreshold: 1
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            successThreshold: 1
            timeoutSeconds: 5
          resources:
            limits:
              memory: 512Mi
              cpu: 200m
            requests:
              cpu: 50m
              memory: 150Mi
          env:
            - name: "DB_BASE"
              value: "/db"
          volumeMounts:
            - mountPath: /db
              name: match-rally-data
      volumes:
        - name: match-rally-data
          persistentVolumeClaim:
            claimName: match-rally-disk
