apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: chipcie-website
  namespace: flux-system
spec:
  image: ghcr.io/wisvch/chipcie-website
  interval: 15m0s
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: chipcie-website
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: chipcie-website
  filterTags:
    pattern: "^[0-9]{8}-(?P<run>[0-9]+)-[a-fA-F0-9]+$"
    extract: "$run"
  policy:
    numerical:
      order: asc
